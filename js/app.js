import React from 'react';
import ReactDOM from 'react-dom';
import Bech32Address from 'Bech32Address';

ReactDOM.render(
  React.createElement(Bech32Address),
  document.getElementById('container')
);
